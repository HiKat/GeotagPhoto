# GeotagPhoto SPEC

## 1. 現在の最新仕様 [v1.2.3]

GeotagPhoto は、Garmin Connect または手動配置した GPX/TCX ファイルを使い、写真へ位置情報を付与する Windows 向け GUI アプリです。画像は元ファイルを保持したまま取り込み先へコピーし、ExifTool によりジオタグを付与します。

### 1.1 メイン画面 [v1.2.3]

- `メイン` タブの内容はスクロール可能なフレーム内に配置します。
- 画面高さが不足する場合でも、ディレクトリ選択、アクティビティダウンロード、`⑥ 取り込み` ボタンへ縦スクロールで到達できます。
- 右側の地図エリアもスクロール可能なフレーム内に配置します。
- 地図タイトル、地図、説明文、マップのリロードボタンは右側エリア全体として上下に確認できます。
- 地図上のマウスホイール操作は従来どおり地図ズームを優先します。地図エリア全体の縦スクロールは地図外またはスクロールバーで操作します。

### 1.2 地図表示 [v1.2.3]

- 地図は `TkinterMapView` を利用して表示します。
- 写真の撮影位置はマーカーとして表示します。
- 設定に応じて GPX/TCX の移動軌跡を地図上に描画します。
- 地図設定を保存した場合は、既存の地図ウィジェットを破棄して再生成します。再生成後も右側エリアのスクロール構造を維持します。
- 地図ウィジェットは CustomTkinter のスクロールフレームへ直接配置せず、通常の `tk.Frame` を介して配置します。これにより、地図ウィジェット生成時の背景色参照エラーを避けます。

### 1.3 取り込み処理 [v1.2.3]

- 取り込み元フォルダから対象画像、RAW、動画ファイルを取り込み先フォルダへコピーします。
- コピー元ファイルが存在しない場合やコピーに失敗した場合は、コピー元とコピー先のフルパスをログへ出力します。
- コピー完了時は成功件数と失敗件数をログへ出力します。
- 取り込み開始前に取り込み先フォルダへ一時ファイルを書き込み、実際に書き込み可能か確認します。
- 画像取り込み開始前は、アプリ本体に加えて ExifTool からも取り込み先へ一時画像を書き込み、ジオタグ更新に必要な権限を確認します。
- ExifTool の書き込み終了コードが 1 以上の場合は失敗として扱い、並列バッチ処理中の失敗も成功扱いせず呼び出し元へ通知します。メタデータ読み取り時に限り、ExifTool の終了コード 1 を警告付き成功として扱えます。
- 書き込み確認または実際のジオタグ更新に失敗した場合は、コピーや処理を続行せず解決用ポップアップを自動表示します。
- 処理完了時のジオタグ付与件数は、出力画像を再読込した実結果を基に表示します。写真の撮影時刻に一致する軌跡がない件数も表示します。

### 1.4 GPX/TCX 取得と保存 [v1.2.3]

- Garmin Connect から指定日のアクティビティを GPX または TCX としてダウンロードできます。
- Strava から取得したストリームデータを GPX として保存できます。
- Garmin/Strava の GPX/TCX 保存処理前に、保存先フォルダへ一時ファイルを書き込み、実際に書き込み可能か確認します。
- 書き込み確認に失敗した場合はダウンロード処理を開始せず、解決用ポップアップを自動表示します。
- 手動で配置した GPX/TCX ファイルも、指定した位置情報ファイル保存先から利用できます。

### 1.5 Windows Defender 対応 [v1.2.3]

- 取り込み先または GPX/TCX 保存先が Windows Defender の「コントロールされたフォルダー アクセス」により保護されている場合、Python/EXE からの書き込みがブロックされることがあります。
- アプリ側では事前の一時ファイル書き込み確認により、コピー、ダウンロード、ジオタグ更新の途中で分かりにくいエラーが発生する前に原因を示します。
- 書き込み確認に失敗した場合は解決用ポップアップを自動表示し、「Defender の許可に追加」「別の保存先を選択」「書き込みを再チェック」を同じ画面から実行できます。
- 「Defender の許可に追加」は UAC 確認付きの非表示管理者 PowerShell を起動し、現在実行中の `GeotagPhoto.exe`、もしくは `python.exe` を許可アプリへ追加します。画像取り込み時は `exiftool.exe` も同時に追加します。
- 許可追加処理の完了を待って成否をポップアップへ反映し、完了後に「書き込みを再チェック」を押すよう案内します。
- 「書き込みを再チェック」はアプリ本体と ExifTool の両方から対象フォルダへ実際に書き込み、ブロックが解消されたことを確認します。
- 「別の保存先を選択」は設定ファイルとメイン画面の入力欄を更新し、選択先の書き込み可否をその場で確認します。
- 許可追加は既存の許可アプリを維持したまま `Add-MpPreference` で実行し、組織ポリシーや Microsoft Defender の管理状態により失敗した場合は手動実行用コマンドをログへ表示します。

### 1.6 配布物 [v1.2.3]

- Windows 向け配布物は Nuitka の standalone ビルドで作成します。
- CPU 互換性を優先し、Nuitka は `--msvc=latest` を使用します。
- Zig バックエンドおよび `--lto=yes` は、過去に `0xc000001d` の原因となったためリリースビルドでは使用しません。
- `GeotagPhoto.exe` は同じフォルダ内の DLL やデータファイルを参照するため、配布フォルダ全体を展開して利用します。
- `GeotagPhoto.exe` のファイルアイコンには `static/logo/app.ico` を埋め込みます。
- `exiftool.exe` は同梱せず、README の手順に従って利用者が別途インストールします。
- 候補版の機械検証後、ローカルの `releases/latest` を最新の検証済み候補版ディレクトリへのシンボリックリンクとして更新します。旧版への逆行は拒否し、公開後検証ではリンクを変更しません。GitHub での公開状態はタグと GitHub Release で別途確認します。

## 2. 変更された実装 [v1.2.3]

- 書き込みエラー時に解決用ポップアップを自動表示し、Defender の許可追加、保存先変更、書き込み再チェックを同じ画面から実行できるようにしました。
- Defender の許可追加は UAC 付きの非表示管理者 PowerShell で完了を待ち、処理結果と次の操作をポップアップに表示するようにしました。
- 画像取り込み時は `GeotagPhoto.exe`、もしくは `python.exe` に加えて `exiftool.exe` も許可対象とし、再チェック時にアプリ本体と ExifTool の両方から実書き込みを確認するようにしました。
- ExifTool の書き込み終了コード 1 を失敗として扱い、並列バッチ内の失敗も呼び出し元へ伝播するようにしました。
- ジオタグ付与件数を処理後の画像から再集計し、撮影時刻に一致する軌跡がない件数も表示するようにしました。
- 候補版の機械検証後に `releases/latest` を更新し、リンク先、EXE バージョン、配布フォルダーと ZIP の全ファイル SHA-256、タグ、GitHub 公開物の整合を公開後にも再検証する手順へ変更しました。

## 3. 削除された実装 [v1.2.3]

- v1.2.3 ではユーザー向け機能の削除はありません。

## 4. ビルド方針 [v1.2.3]

- ビルド元ブランチは `dev` とします。
- ローカル成果物は `releases\v1.2.3` に作成します。
- Nuitka の主な指定は次のとおりです。
  - `--standalone`
  - `--enable-plugin=tk-inter`
  - `--msvc=latest`
  - `--windows-console-mode=disable`
  - `--windows-file-version=1.2.3.0`
  - `--windows-product-version=1.2.3.0`
  - `--windows-icon-from-ico=static/logo/app.ico`
- `COPYING`, `NOTICE.md`, `THIRD_PARTY_NOTICES.md`, `third_party_licenses/`, `README.txt` を配布フォルダに同梱します。
- `README.txt` はリポジトリの `README.md` をコピーして作成します。
- README の最新リリースリンクはビルド前に更新して `README.txt` へ反映し、タグ対象コミットへ含めます。GitHub Release 公開、タグ作成、main へのマージはローカル成果物レビュー後の別工程とします。

## 5. リリース前チェック [v1.2.3]

- 認証情報、個人設定、トークン、ローカル環境ファイルが追跡対象に混入していないことを確認します。
- `myenv` の Python で `main.py` の構文チェックを実行します。
- `myenv` の Python で `test/` と `tests/` 配下の検証を実行します。
- `README.md` と `SPEC.md` に v1.2.3 の変更内容と注意事項が反映されていることを確認します。
- Nuitka ビルド後、同梱スクリプトで EXE バージョン、配布フォルダーと ZIP の全ファイル SHA-256、ZIP の安全なパス構造、`releases/latest` のリンク先を機械検証します。公開後はタグ内文書、リモート参照、GitHub Release 本文、ダウンロードした ZIP の SHA-256 も検証します。

## 6. 既知の注意事項 [v1.2.3]

- Windows Defender の「コントロールされたフォルダー アクセス」が有効な環境では、一般的なフォルダ名であっても、保護対象フォルダ配下であれば書き込みがブロックされる場合があります。
- `where cl` は通常の PowerShell では見つからない場合があります。Visual Studio Build Tools がインストールされていれば、Nuitka の `--msvc=latest` により MSVC を検出できます。
- AVX-512 非対応 CPU での `0xc000001d` 再発確認は、該当 CPU を持つ実機での追加確認が必要です。

## 7. リリース履歴

### v1.2.3 [v1.2.3]

- 書き込み失敗時に解決用ポップアップを自動表示し、Defender の許可追加、別の保存先選択、書き込み再チェックを一画面で行えるようにしました。
- Defender の許可追加後に「書き込みを再チェック」を押すよう明示し、アプリ本体と ExifTool の両方を確認するようにしました。
- ExifTool が Defender にブロックされた場合などの書き込み失敗を成功扱いせず、並列バッチからもエラーを通知するようにしました。
- ジオタグ付与件数を実結果から再集計し、撮影時刻に一致する軌跡がない写真の件数も表示するようにしました。
- リリース手順に、検証済み候補版だけを `releases/latest` に反映し、公開後はリンクを変更せず Git・GitHub・配布物の整合を再検証する工程を追加しました。

### v1.2.2 [v1.2.2]

- Windows Defender の許可アプリ追加を、書き込みエラー画面から UAC 付き管理者 PowerShell で起動できるようにしました。
- ソース実行時は、仮想環境の `python.exe` と Python 本体が異なる場合に両方を許可対象へ追加するようにしました。
- README 冒頭にアプリアイコンを配置し、最新リリースリンクを v1.2.2 に更新しました。
- リリースビルドの EXE に `static/logo/app.ico` を埋め込む方針を明記しました。

### v1.2.1 [v1.2.1]

- メインタブと右側地図エリアの縦スクロールに対応しました。
- 取り込み先と GPX/TCX 保存先の書き込み可否を処理開始前に確認するようにしました。
- Windows Defender の「コントロールされたフォルダー アクセス」が疑われる場合のエラーメッセージと README の案内を改善しました。
- Windows Defender の許可アプリ追加を、書き込みエラー画面から UAC 付き管理者 PowerShell で起動できるようにしました。
- コピー失敗時のログを詳細化し、成功件数と失敗件数を表示するようにしました。
- 地図再生成後も右側スクロール構造を維持するようにしました。
- ローカルエージェント向けの AGENTS.md とシンボリックリンク作成スクリプトを追加しました。
- リリースビルドは CPU 互換性のため `--msvc=latest` を必須とし、Zig バックエンドと LTO を使用しない方針を明記しました。
